//Creazione_Database

1)CREATE SCHEMA `auctionlinedb` ;
2)Imposta auctionlinedb come default schema;
3)copy-paste:

CREATE TABLE IF NOT EXISTS asta 
(
	Titolo VARCHAR(30) NOT NULL,
	Descrizione VARCHAR(150),
	Categoria ENUM ('TabletTelefonia', 'Giocattoli', 'Elettronica', 'Arte', 'Immobili', 'Antiquariato') NOT NULL,
	Tipologia ENUM ('astaTempoFisso', 'astaInglese') NOT NULL,
	DataInizio DATETIME NOT NULL,
	OffertaIniziale float NOT NULL,
	OffertaPiuAlta float,
	IDOffertaPiuAlta int,
	FotoAsta1 varchar(500),
	FotoAsta2 varchar(500),
	FotoAsta3 varchar(500),
	FotoAsta4 varchar(500),
	FotoAsta5 varchar(500), 

	-- Per aste inglesi
	Timer TIME,
	SogliaRialzo float,

	-- Per aste tempofisso
	DataFine DATETIME,


	-- PK e FK  
	IDasta INT AUTO_INCREMENT NOT NULL,
	ProprietarioFK int NOT NULL,
    
	PRIMARY KEY (IDasta)
    
  );


CREATE TABLE IF NOT EXISTS utente 
(
	Nome VARCHAR(30) NOT NULL,
	Cognome VARCHAR(30) NOT NULL,
	Biografia VARCHAR(1000),
 	Geolocalizzazione VARCHAR(50) ,
 	ElencoSocial VARCHAR(1000),
 	FotoProfilo VARCHAR(1000),
	EmailUtente varchar(40) NOT NULL,
 	PasswordUtente varchar(30)NOT NULL,

	IDUtente INT AUTO_INCREMENT NOT NULL,

  	PRIMARY KEY (IDUtente)
  
  );


CREATE TABLE IF NOT EXISTS partecipazioneutenteasta
(
	IDUtente INT,
   	IDAsta INT,
    
	PRIMARY KEY (IDUtente, IDAsta)
	
);


CREATE TABLE IF NOT EXISTS notifiche 
(	
	Titolo varchar(50) NOT NULL,
	Testo varchar(150) NOT NULL,
    	
	IDnotifiche INT AUTO_INCREMENT NOT NULL,
    	IDutenteFK int NOT NULL,

	PRIMARY KEY (IDnotifiche)
);
    

ALTER TABLE auctionlinedb.ASTA
add FOREIGN KEY (ProprietarioFK) REFERENCES auctionlinedb.utente(IDUtente);

ALTER TABLE auctionlinedb.partecipazioneUtenteAsta
add FOREIGN KEY (IDAsta) REFERENCES asta(IDAsta),
add FOREIGN KEY (IDUtente) REFERENCES utente(IDUtente);

ALTER TABLE auctionlinedb.notifiche
add FOREIGN KEY (IDUtenteFK) REFERENCES utente(IDUtente);